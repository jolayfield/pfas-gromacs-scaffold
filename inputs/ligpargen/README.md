# LigParGen Input Files

Put one `.gro` and one `.itp` file per solute molecule in this directory.

Then edit `inputs/molecules.csv` so each row points to the correct files and uses the molecule name from the `.itp` `[ moleculetype ]` section.

